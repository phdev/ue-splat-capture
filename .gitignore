# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Tooling caches
.pytest_cache/
.ruff_cache/
.DS_Store

# Working/scratch output (NOT committed). Fixtures are committed; out/ is not.
out/

# Verification results are regenerated by `make verify`. We commit ONLY the
# frozen baseline; per-run tier JSONs are ignored.
results/*.json
!results/baseline.json
